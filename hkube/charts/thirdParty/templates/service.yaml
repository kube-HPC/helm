apiVersion: v1
kind: Service
metadata:
  name: {{ template "thirdParty.fullname" . }}
  labels:
    app: {{ template "thirdParty.name" . }}
    chart: {{ template "thirdParty.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "thirdParty.name" . }}
    release: {{ .Release.Name }}
