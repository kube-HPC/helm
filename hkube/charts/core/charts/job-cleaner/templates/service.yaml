apiVersion: v1
kind: Service
metadata:
  name: {{ template "job-cleaner.fullname" . }}
  labels:
    app: {{ template "job-cleaner.name" . }}
    chart: {{ template "job-cleaner.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "job-cleaner.name" . }}
    release: {{ .Release.Name }}
