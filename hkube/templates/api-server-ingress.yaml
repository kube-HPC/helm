{{- if index .Values "api-server" "ingress" "enabled" -}}
{{- $fullName := "api-server" -}}
{{- $servicePort := index .Values "api-server" "env" "port" -}}
{{- $ingressPath := index .Values "api-server" "ingress" "path" -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api-server
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  labels:
    app: api-server
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    group: {{ .Values.lables.group.value }}
    core: "true"
spec:
{{- if .Values.ingress.hostname }}
  tls:
  - hosts:
    - {{ .Values.ingress.hostname }}
{{- end }}
  rules:
    - http:
        paths:
        - path: {{ $ingressPath }}
          backend:
            serviceName: {{ $fullName }}
            servicePort: {{ $servicePort }}
{{- if .Values.ingress.hostname }}
      host: {{ .Values.ingress.hostname }}
{{- end }}
{{- end }}