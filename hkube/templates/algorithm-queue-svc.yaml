apiVersion: v1
kind: Service
metadata:
  name: algorithm-queue-svc
  annotations:
    prometheus.io/scrape: "true"
  labels:
    app: algorithm-queue-svc
    chart: algorithm-queue-svc
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    group: {{ .Values.lables.group.value }}
    core: "true"
spec:
  type: {{ index .Values "algorithm-queue-svc" "service" "type" }}
  ports:
    - port: {{ index .Values "algorithm-queue-svc" "env" "metric_port" }}
      targetPort: {{ index .Values "algorithm-queue-svc" "env" "metric_port" }}
      protocol: TCP
  selector:
    metrics-group: 'algorithm-queue'