apiVersion: v1
kind: Service
metadata:
  name: {{ template "hkube.fullname" . }}
  labels:
    app: {{ template "hkube.name" . }}
    chart: {{ template "hkube.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hkube.name" . }}
    release: {{ .Release.Name }}
