{{- if index .Values "pipeline-driver-queue" "ingress" "enabled" -}}
{{- $fullName := "pipeline-driver-queue" -}}
{{- $servicePort := index .Values "pipeline-driver-queue" "env" "metric_port" -}}
{{- $ingressPath := index .Values "pipeline-driver-queue" "ingress" "path" -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: pipeline-driver-queue
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  labels:
    app: pipeline-driver-queue
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    group: {{ .Values.lables.group.value }}
    core: "true"
spec:
  rules:
    - http:
        paths:
        - path: {{ $ingressPath }}
          backend:
            serviceName: {{ $fullName }}
            servicePort: {{ $servicePort }}
{{- end }}
