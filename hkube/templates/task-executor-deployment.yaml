apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: task-executor
  labels:
    app: task-executor
    scale-group: task-executor
    group: {{ .Values.lables.group.value }}
    core: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task-executor
  template:
    metadata:
      labels:
        app: task-executor
        group: {{ .Values.lables.group.value }}
    spec:
      {{- if .Values.global.production }}
      nodeSelector:
        core: "true"
      {{- end }}
      serviceAccountName: task-executor-serviceaccount
      containers:
        - name: task-executor
          image: "{{ .Values.global.registry }}{{ index .Values "task-executor" "image" "repository" }}:{{ index .Values "task-executor" "image" "tag" }}"
          resources:
            limits:
              cpu: 250m
              memory: 512Mi
          ports:
            - containerPort: {{ index .Values "task-executor" "env" "metric_port" }}
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: NODE_ENV
            - name: IS_PRIVILEGED
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: IS_PRIVILEGED
            - name: IS_NAMESPACED
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: IS_NAMESPACED
            - name: PIPELINE_DRIVERS_AMOUNT
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: PIPELINE_DRIVERS_AMOUNT
            - name: METRICS_PORT
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: METRICS_PORT
            - name: JAEGER_AGENT_SERVICE_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: DEFAULT_STORAGE
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: DEFAULT_STORAGE
            - name: BASE_FS_ADAPTER_DIRECTORY
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: BASE_FS_ADAPTER_DIRECTORY
            - name: WORKER_SOCKET_MAX_PAYLOAD_BYTES
              valueFrom:
                configMapKeyRef:
                  name: task-executor-configmap
                  key: WORKER_SOCKET_MAX_PAYLOAD_BYTES
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace

                  