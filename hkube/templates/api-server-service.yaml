apiVersion: v1
kind: Service
metadata:
  name: api-server
  annotations:
    prometheus.io/scrape: "true"
  labels:
    app: api-server
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    group: {{ .Values.lables.group.value }}
    core: "true"
spec:
  type: {{ index .Values "api-server" "service" "type" }}
  ports:
    - port: {{ index .Values "api-server" "env" "port" }}
      targetPort: {{ index .Values "api-server" "env" "port" }}
      protocol: TCP
      name: rest
    - port: {{ index .Values "api-server" "env" "metric_port" }}
      targetPort: {{ index .Values "api-server" "env" "metric_port" }}
      protocol: TCP
      name: metric
    
  selector:
    app: api-server
    release: api-server
