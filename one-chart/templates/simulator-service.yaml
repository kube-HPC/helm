kind: Service
apiVersion: v1
metadata:
  name: simulator
  labels:
    app: simulator
    group: {{ .Values.lables.group.value }}
    core: "true"
spec:
  selector:
    app: simulator
  ports:
    - protocol: TCP
      port: {{ index .Values "simulator" "service" "port" }}
      targetPort: {{ index .Values "simulator" "service" "port" }}