apiVersion: v1
kind: Service
metadata:
  name: {{ template "mini.fullname" . }}
  labels:
    app: {{ template "mini.name" . }}
    chart: {{ template "mini.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "mini.name" . }}
    release: {{ .Release.Name }}
